/// <reference path="jquery.d.ts"/>
interface JQueryStatic {
    scrollTo(options : any);
    scrollTo(target : any, duration : any);
    scrollTo(target : any, duration : any, settings : any);    
}

interface JQuery {
    scrollTo(options : any);
    scrollTo(target : any, duration : any);
    scrollTo(target : any, duration : any, settings : any);    
}