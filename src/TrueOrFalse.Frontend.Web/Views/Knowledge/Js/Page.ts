$(() => {
   var wkc= new WishKnowledgeContent();
    //$(window).on('popstate', function (event) {
 
    //    if (history.state && history.state.id == 'Themen') {
    //        $("#Topics").SetTabActive();
    //        $.ajax({
    //            url: "/Knowledge/GetKnowledgeContent?content=topics",
    //            type: "POST",
    //            beforeSend() { $(".spinner").fadeIn() },
    //        }).done(function (data) {
    //            $(".content").html(data);
    //            $(".LinkIsDirectedToPartialView").text("Thema erstellen");
    //            $(".LinkIsDirectedToPartialView").attr("href", $("#hddUrlAddTopic").val());
    //            $(".spinner").hide();
    //        });
    //    }
    //}, false);

    //$(window).on('popstate', function (e) {
    //    var state = e.originalEvent.state;
    //    if (state === "Themen") {
    //        $.ajax({
    //            url: "/Knowledge/GetKnowledgeContent?content=topics",
    //            type: "POST",
    //            beforeSend() { $(".spinner").fadeIn() },
    //        }).done(function (data) {
    //            $(".content").html(data);
    //            $(".LinkIsDirectedToPartialView").text("Thema erstellen");
    //            $(".LinkIsDirectedToPartialView").attr("href", $("#hddUrlAddTopic").val());
    //            $(".spinner").hide();

    //            wkc.SetTabActiveString("#topics");
    //        });
    //    }
    //});


});

