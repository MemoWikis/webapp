using System;
using System.Collections.Generic;

public class GetNetworkActivity
{

    public IList<UserActivity> Run(int userId, int amount)
    {
        //var user = Sl.R<UserRepo>().GetById(userId);
        //var followerIAm = Sl.R<FollowerIAm>().Init(user.Following.Select(u => u.Id), userId);
        return null;
    }
}
